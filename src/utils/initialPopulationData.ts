import { PopulationData } from '../type/types';

// 無選択時にもグラフが表示されるようにするための初期データ
export const initialPrefData: PopulationData = {
  id: 0,
  label: '都道府県',
  data: [],
  borderColor: 'transparent',
};
